# Test Accounts - UniBite Platform

## Admin Account (Testing)
- **Username**: `admin`
- **Password**: `admin`
- **Access**: Admin Dashboard with full statistics, leaderboard, top meals, activity feed
- **Features**: 
  - View platform statistics (total portions, users, avg rating, meals)
  - View leaderboard of top donors
  - View top-rated meals
  - View recent activity feed

## Regular User Accounts (Create on Login)
Regular users can create accounts by simply entering any username (e.g., `@chef_john`, `@maria_cook`, etc.) without a predefined password. The system will automatically create a profile on first login.

## Testing Instructions
1. Open `index.html`
2. Click "Login" button in header
3. For **Admin Dashboard**: Enter username `admin` and password `admin`
4. For **Regular User**: Enter any username (will auto-create profile)
5. Users can then:
   - Upload meals
   - Browse feed
   - Send requests
   - Rate meals
   - View notifications
